// Constants for the website. Some of these are also declared in the next-sitemap.config.js file.

export const SITE_URL = (process.env.BASE_URL || 'https://www.helix-db.com').replace(/\/$/, '')
export const DOCS_URL = (process.env.DOCS_URL || 'https://docs.helix-db.com').replace(/\/$/, '')
