
export default function NewQuery({ params }: { params: Promise<{ instanceId: string, queryId: string }> }) {
    return (
        <div className="flex flex-col h-screen overflow-hidden">
            {/* Query editor content will go here */}
        </div>
    );
}
